;; Org Agenda log mode
(setq org-agenda-start-with-log-mode t)
(setq org-log-done 'time)
(setq org-log-into-drawer t)

(provide 'shapes-module-org-agenda-log-mode)
;;; shapes-org-agenda-log-mode.el ends here
